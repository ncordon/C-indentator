#include <iostream>

void funcion(){
    if(algo)
    { 
        int hola;
        if(hola)
            int prueba;
        else(otro_algo)
            if(foo){
                int bar;
            }
            else if(foo)
                int otro;
            else if(bar){
                if(foo)
                    int hola;
                else{
                    float otro;
                }    
            }
            else if(condicion)
                if(condicion)
                    if(condicion)
                        if(condicion)
                            if(condicion)
                                int esto;
                            else{
                                int otro;}

    }}